# 香水数据维护

香水列表在 `perfumes.json`。后续扩充时，复制一条完整记录，改掉 `id`、`slug`、品牌、名称、香调、标签和视觉色板即可。

固定筛选词表在 `taxonomy.ts`。主要香材会自动从香水数据里生成；香调分类会合并词表和数据中实际出现的分类；风格标签建议先补进词表，再写入香水记录。

加完数据后运行：

```bash
pnpm validate:data
pnpm build
```

评分没有确认时继续写 `"待补充"`，不要为了占位填写不确定的分数。
